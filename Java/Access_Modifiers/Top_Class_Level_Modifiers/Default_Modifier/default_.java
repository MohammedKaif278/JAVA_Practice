package Access_Modifiers.Top_Class_Level_Modifiers.Default_Modifier;

 class default_
 {
     /*
     Default Access Modifier :-
        If the class is declared as default then we access that class within the
        same package only , hence default access is also known as package level class

      */
}
